Global "Quiz Must-Haves" checklist (final)
For every new quiz module, shipping is allowed ONLY if:

IDs: registry-lint passes without adding new allowlist prefixes

Event: assertValidContributionEvent passes

Output: payload.markers is non-empty

Bounds: ingestion -> snapshot traits in 1..100

Tier: correct tier + marker budgets respected

UX: progress + result card + share + CTA to /character

Static: dual-mode contribute works (API + static export)